# Synthetic v3 record fixtures

This entire tree is test-only and is not addressable from an Aegis content
manifest. Its numeric values are deliberately small, distinct synthetic
sentinels for schema and cross-reference tests. They are never Candidate-BAL
balance values and must not be copied into candidate or production content.
